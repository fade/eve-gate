;;;; universe.lisp - Generated ESI API functions for universe endpoints
;;;;
;;;; This file is AUTO-GENERATED by the eve-gate code generation framework.
;;;; Do not edit manually. Changes will be overwritten on regeneration.
;;;;
;;;; Category: universe
;;;; Endpoints: 31
;;;; Generated: 2026-04-12 21:54:04 UTC

(in-package :eve-gate.api)


(defun get-universe-ancestries (client &key (language "en"))
  "Get all character ancestries

ESI Endpoint: GET /universe/ancestries/
Operation ID: get_universe_ancestries
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: ARRAY of OBJECT
Response: A list of ancestries

Example:
  (get-universe-ancestries client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path "/universe/ancestries/")
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-asteroid-belts-asteroid-belt-id (client asteroid-belt-id &key)
  "Get information on an asteroid belt

ESI Endpoint: GET /universe/asteroid_belts/{asteroid_belt_id}/
Operation ID: get_universe_asteroid_belts_asteroid_belt_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  ASTEROID-BELT-ID (INTEGER/INT32) - Required. asteroid_belt_id integer

Response Type: OBJECT
Response: Information about an asteroid belt

Example:
  (get-universe-asteroid-belts-asteroid-belt-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type asteroid-belt-id integer)
      (unless (<= -2147483648 asteroid-belt-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "asteroid_belt_id" asteroid-belt-id
                       -2147483648 2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/asteroid_belts/{asteroid_belt_id}/"
                                      (list
                                       (cons "asteroid_belt_id"
                                             (princ-to-string asteroid-belt-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-bloodlines (client &key (language "en"))
  "Get a list of bloodlines

ESI Endpoint: GET /universe/bloodlines/
Operation ID: get_universe_bloodlines
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: ARRAY of OBJECT
Response: A list of bloodlines

Example:
  (get-universe-bloodlines client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path "/universe/bloodlines/")
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-categories (client &key)
  "Get a list of item categories

ESI Endpoint: GET /universe/categories/
Operation ID: get_universe_categories
Category: universe
Authentication: Not required (public endpoint)

Response Type: ARRAY of INTEGER
Response: A list of item category ids

Example:
  (get-universe-categories client)"
  (let ((request-path "/universe/categories/"))
    (let ((response (http-request client request-path :method :get)))
      (values (esi-response-body response) response))))


(defun get-universe-categories-category-id (client category-id &key (language "en"))
  "Get information of an item category

ESI Endpoint: GET /universe/categories/{category_id}/
Operation ID: get_universe_categories_category_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  CATEGORY-ID (INTEGER/INT32) - Required. An Eve item category ID
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: OBJECT
Response: Information about an item category

Example:
  (get-universe-categories-category-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type category-id integer)
      (unless (<= -2147483648 category-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "category_id" category-id -2147483648
                       2147483647))))
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path
          (substitute-path-parameters "/universe/categories/{category_id}/"
                                      (list (cons "category_id" (princ-to-string category-id)))))
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-constellations (client &key)
  "Get a list of constellations

ESI Endpoint: GET /universe/constellations/
Operation ID: get_universe_constellations
Category: universe
Authentication: Not required (public endpoint)

Response Type: ARRAY of INTEGER
Response: A list of constellation ids

Example:
  (get-universe-constellations client)"
  (let ((request-path "/universe/constellations/"))
    (let ((response (http-request client request-path :method :get)))
      (values (esi-response-body response) response))))


(defun get-universe-constellations-constellation-id (client constellation-id &key (language "en"))
  "Get information on a constellation

ESI Endpoint: GET /universe/constellations/{constellation_id}/
Operation ID: get_universe_constellations_constellation_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  CONSTELLATION-ID (INTEGER/INT32) - Required. constellation_id integer
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: OBJECT
Response: Information about a constellation

Example:
  (get-universe-constellations-constellation-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type constellation-id integer)
      (unless (<= -2147483648 constellation-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "constellation_id" constellation-id
                       -2147483648 2147483647))))
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path
          (substitute-path-parameters "/universe/constellations/{constellation_id}/"
                                      (list
                                       (cons "constellation_id"
                                             (princ-to-string constellation-id)))))
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-factions (client &key (language "en"))
  "Get a list of factions

ESI Endpoint: GET /universe/factions/
Operation ID: get_universe_factions
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: ARRAY of OBJECT
Response: A list of factions

Example:
  (get-universe-factions client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path "/universe/factions/")
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-graphics (client &key)
  "Get a list of graphics

ESI Endpoint: GET /universe/graphics/
Operation ID: get_universe_graphics
Category: universe
Authentication: Not required (public endpoint)

Response Type: ARRAY of INTEGER
Response: A list of graphic ids

Example:
  (get-universe-graphics client)"
  (let ((request-path "/universe/graphics/"))
    (let ((response (http-request client request-path :method :get)))
      (values (esi-response-body response) response))))


(defun get-universe-graphics-graphic-id (client graphic-id &key)
  "Get information on a graphic

ESI Endpoint: GET /universe/graphics/{graphic_id}/
Operation ID: get_universe_graphics_graphic_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  GRAPHIC-ID (INTEGER/INT32) - Required. graphic_id integer

Response Type: OBJECT
Response: Information about a graphic

Example:
  (get-universe-graphics-graphic-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type graphic-id integer)
      (unless (<= -2147483648 graphic-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "graphic_id" graphic-id -2147483648
                       2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/graphics/{graphic_id}/"
                                      (list (cons "graphic_id" (princ-to-string graphic-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-groups (client &key (page 1))
  "Get a list of item groups

ESI Endpoint: GET /universe/groups/
Operation ID: get_universe_groups
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :PAGE (INTEGER/INT32) - Optional. Which page of results to return [default: 1]

Pagination: This endpoint supports pagination.
  Use :PAGE to request a specific page (1-indexed).
  Returns: (VALUES data total-pages response)

Response Type: ARRAY of INTEGER
Response: A list of item group ids

Example:
  (get-universe-groups client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when page (check-type page integer))
      (when page
        (unless (<= 1 page 2147483647)
          (error 'esi-bad-request :message
                 (format nil "~A: ~D out of range [~D, ~D]" "page" page 1 2147483647))))))
   (let ((request-path "/universe/groups/")
         (query-params (remove nil (list (when page (cons "page" (princ-to-string page)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response)
               (let ((headers (esi-response-headers response)))
                 (when headers
                   (multiple-value-bind (etag expires pages)
                       (extract-esi-metadata headers)
                     (declare (ignore etag expires))
                     pages)))
               response)))))


(defun get-universe-groups-group-id (client group-id &key (language "en"))
  "Get information on an item group

ESI Endpoint: GET /universe/groups/{group_id}/
Operation ID: get_universe_groups_group_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  GROUP-ID (INTEGER/INT32) - Required. An Eve item group ID
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: OBJECT
Response: Information about an item group

Example:
  (get-universe-groups-group-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type group-id integer)
      (unless (<= -2147483648 group-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "group_id" group-id -2147483648
                       2147483647))))
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path
          (substitute-path-parameters "/universe/groups/{group_id}/"
                                      (list (cons "group_id" (princ-to-string group-id)))))
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-moons-moon-id (client moon-id &key)
  "Get information on a moon

ESI Endpoint: GET /universe/moons/{moon_id}/
Operation ID: get_universe_moons_moon_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  MOON-ID (INTEGER/INT32) - Required. moon_id integer

Response Type: OBJECT
Response: Information about a moon

Example:
  (get-universe-moons-moon-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type moon-id integer)
      (unless (<= -2147483648 moon-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "moon_id" moon-id -2147483648
                       2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/moons/{moon_id}/"
                                      (list (cons "moon_id" (princ-to-string moon-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-planets-planet-id (client planet-id &key)
  "Get information on a planet

ESI Endpoint: GET /universe/planets/{planet_id}/
Operation ID: get_universe_planets_planet_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  PLANET-ID (INTEGER/INT32) - Required. planet_id integer

Response Type: OBJECT
Response: Information about a planet

Example:
  (get-universe-planets-planet-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type planet-id integer)
      (unless (<= -2147483648 planet-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "planet_id" planet-id -2147483648
                       2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/planets/{planet_id}/"
                                      (list (cons "planet_id" (princ-to-string planet-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-races (client &key (language "en"))
  "Get a list of character races

ESI Endpoint: GET /universe/races/
Operation ID: get_universe_races
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: ARRAY of OBJECT
Response: A list of character races

Example:
  (get-universe-races client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path "/universe/races/")
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-regions (client &key)
  "Get a list of regions

ESI Endpoint: GET /universe/regions/
Operation ID: get_universe_regions
Category: universe
Authentication: Not required (public endpoint)

Response Type: ARRAY of INTEGER
Response: A list of region ids

Example:
  (get-universe-regions client)"
  (let ((request-path "/universe/regions/"))
    (let ((response (http-request client request-path :method :get)))
      (values (esi-response-body response) response))))


(defun get-universe-regions-region-id (client region-id &key (language "en"))
  "Get information on a region

ESI Endpoint: GET /universe/regions/{region_id}/
Operation ID: get_universe_regions_region_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  REGION-ID (INTEGER/INT32) - Required. region_id integer
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: OBJECT
Response: Information about a region

Example:
  (get-universe-regions-region-id client 10000002)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type region-id integer)
      (unless (<= -2147483648 region-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "region_id" region-id -2147483648
                       2147483647))))
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path
          (substitute-path-parameters "/universe/regions/{region_id}/"
                                      (list (cons "region_id" (princ-to-string region-id)))))
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-schematics-schematic-id (client schematic-id &key)
  "Get information on a planetary factory schematic

ESI Endpoint: GET /universe/schematics/{schematic_id}/
Operation ID: get_universe_schematics_schematic_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  SCHEMATIC-ID (INTEGER/INT32) - Required. A PI schematic ID

Response Type: OBJECT
Response: Public data about a schematic

Cache Duration: 3600 seconds

Example:
  (get-universe-schematics-schematic-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type schematic-id integer)
      (unless (<= -2147483648 schematic-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "schematic_id" schematic-id -2147483648
                       2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/schematics/{schematic_id}/"
                                      (list (cons "schematic_id" (princ-to-string schematic-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-stargates-stargate-id (client stargate-id &key)
  "Get information on a stargate

ESI Endpoint: GET /universe/stargates/{stargate_id}/
Operation ID: get_universe_stargates_stargate_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  STARGATE-ID (INTEGER/INT32) - Required. stargate_id integer

Response Type: OBJECT
Response: Information about a stargate

Example:
  (get-universe-stargates-stargate-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type stargate-id integer)
      (unless (<= -2147483648 stargate-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "stargate_id" stargate-id -2147483648
                       2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/stargates/{stargate_id}/"
                                      (list (cons "stargate_id" (princ-to-string stargate-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-stars-star-id (client star-id &key)
  "Get information on a star

ESI Endpoint: GET /universe/stars/{star_id}/
Operation ID: get_universe_stars_star_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  STAR-ID (INTEGER/INT32) - Required. star_id integer

Response Type: OBJECT
Response: Information about a star

Example:
  (get-universe-stars-star-id client 12345)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type star-id integer)
      (unless (<= -2147483648 star-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "star_id" star-id -2147483648
                       2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/stars/{star_id}/"
                                      (list (cons "star_id" (princ-to-string star-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-stations-station-id (client station-id &key)
  "Get information on a station

ESI Endpoint: GET /universe/stations/{station_id}/
Operation ID: get_universe_stations_station_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  STATION-ID (INTEGER/INT32) - Required. station_id integer

Response Type: OBJECT
Response: Information about a station

Example:
  (get-universe-stations-station-id client 60003760)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type station-id integer)
      (unless (<= -2147483648 station-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "station_id" station-id -2147483648
                       2147483647)))))
   (let ((request-path
          (substitute-path-parameters "/universe/stations/{station_id}/"
                                      (list (cons "station_id" (princ-to-string station-id))))))
     (let ((response (http-request client request-path :method :get)))
       (values (esi-response-body response) response)))))


(defun get-universe-structures (client &key (filter nil))
  "List all public structures

ESI Endpoint: GET /universe/structures/
Operation ID: get_universe_structures
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :FILTER (STRING) - Optional. Only list public structures that have this service online [values: \"market\", \"manufacturing_basic\"]

Response Type: ARRAY of INTEGER
Response: List of public structure IDs

Cache Duration: 3600 seconds

Example:
  (get-universe-structures client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when filter (check-type filter string))
      (when filter
        (unless (member filter '("market" "manufacturing_basic") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "filter" filter
                         '("market" "manufacturing_basic")))))))
   (let ((request-path "/universe/structures/")
         (query-params (remove nil (list (when filter (cons "filter" (princ-to-string filter)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-structures-structure-id (client structure-id &key (token nil))
  "Returns information on requested structure if you are on the ACL. Otherwise, returns \"Forbidden\" for all inputs.

ESI Endpoint: GET /universe/structures/{structure_id}/
Operation ID: get_universe_structures_structure_id
Category: universe
Authentication: Required
Required Scopes: esi-universe.read_structures.v1

Parameters:
  STRUCTURE-ID (INTEGER/INT64) - Required. An Eve structure ID

Response Type: OBJECT
Response: Data about a structure

Cache Duration: 3600 seconds

Example:
  (get-universe-structures-structure-id client 12345)"
  (progn
   (progn (when *validate-parameters-p* (check-type structure-id integer)))
   (let ((request-path
          (substitute-path-parameters "/universe/structures/{structure_id}/"
                                      (list (cons "structure_id" (princ-to-string structure-id))))))
     (let ((response (http-request client request-path :method :get :bearer-token token)))
       (values (esi-response-body response) response)))))


(defun get-universe-system-jumps (client &key)
  "Get the number of jumps in solar systems within the last hour ending at the timestamp of the Last-Modified header, excluding wormhole space. Only systems with jumps will be listed

ESI Endpoint: GET /universe/system_jumps/
Operation ID: get_universe_system_jumps
Category: universe
Authentication: Not required (public endpoint)

Response Type: ARRAY of OBJECT
Response: A list of systems and number of jumps

Cache Duration: 3600 seconds

Example:
  (get-universe-system-jumps client)"
  (let ((request-path "/universe/system_jumps/"))
    (let ((response (http-request client request-path :method :get)))
      (values (esi-response-body response) response))))


(defun get-universe-system-kills (client &key)
  "Get the number of ship, pod and NPC kills per solar system within the last hour ending at the timestamp of the Last-Modified header, excluding wormhole space. Only systems with kills will be listed

ESI Endpoint: GET /universe/system_kills/
Operation ID: get_universe_system_kills
Category: universe
Authentication: Not required (public endpoint)

Response Type: ARRAY of OBJECT
Response: A list of systems and number of ship, pod and NPC kills

Cache Duration: 3600 seconds

Example:
  (get-universe-system-kills client)"
  (let ((request-path "/universe/system_kills/"))
    (let ((response (http-request client request-path :method :get)))
      (values (esi-response-body response) response))))


(defun get-universe-systems (client &key)
  "Get a list of solar systems

ESI Endpoint: GET /universe/systems/
Operation ID: get_universe_systems
Category: universe
Authentication: Not required (public endpoint)

Response Type: ARRAY of INTEGER
Response: A list of solar system ids

Example:
  (get-universe-systems client)"
  (let ((request-path "/universe/systems/"))
    (let ((response (http-request client request-path :method :get)))
      (values (esi-response-body response) response))))


(defun get-universe-systems-system-id (client system-id &key (language "en"))
  "Get information on a solar system.

ESI Endpoint: GET /universe/systems/{system_id}/
Operation ID: get_universe_systems_system_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  SYSTEM-ID (INTEGER/INT32) - Required. system_id integer
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: OBJECT
Response: Information about a solar system

Example:
  (get-universe-systems-system-id client 30000142)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type system-id integer)
      (unless (<= -2147483648 system-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "system_id" system-id -2147483648
                       2147483647))))
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path
          (substitute-path-parameters "/universe/systems/{system_id}/"
                                      (list (cons "system_id" (princ-to-string system-id)))))
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun get-universe-types (client &key (page 1))
  "Get a list of type ids

ESI Endpoint: GET /universe/types/
Operation ID: get_universe_types
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :PAGE (INTEGER/INT32) - Optional. Which page of results to return [default: 1]

Pagination: This endpoint supports pagination.
  Use :PAGE to request a specific page (1-indexed).
  Returns: (VALUES data total-pages response)

Response Type: ARRAY of INTEGER
Response: A list of type ids

Example:
  (get-universe-types client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when page (check-type page integer))
      (when page
        (unless (<= 1 page 2147483647)
          (error 'esi-bad-request :message
                 (format nil "~A: ~D out of range [~D, ~D]" "page" page 1 2147483647))))))
   (let ((request-path "/universe/types/")
         (query-params (remove nil (list (when page (cons "page" (princ-to-string page)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response)
               (let ((headers (esi-response-headers response)))
                 (when headers
                   (multiple-value-bind (etag expires pages)
                       (extract-esi-metadata headers)
                     (declare (ignore etag expires))
                     pages)))
               response)))))


(defun get-universe-types-type-id (client type-id &key (language "en"))
  "Get information on a type

ESI Endpoint: GET /universe/types/{type_id}/
Operation ID: get_universe_types_type_id
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  TYPE-ID (INTEGER/INT32) - Required. An Eve item type ID
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]

Response Type: OBJECT
Response: Information about a type

Example:
  (get-universe-types-type-id client 34)"
  (progn
   (progn
    (when *validate-parameters-p*
      (check-type type-id integer)
      (unless (<= -2147483648 type-id 2147483647)
        (error 'esi-bad-request :message
               (format nil "~A: ~D out of range [~D, ~D]" "type_id" type-id -2147483648
                       2147483647))))
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es")))))))
   (let ((request-path
          (substitute-path-parameters "/universe/types/{type_id}/"
                                      (list (cons "type_id" (princ-to-string type-id)))))
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response (http-request client request-path :method :get :query-params query-params)))
       (values (esi-response-body response) response)))))


(defun post-universe-ids (client &key (language "en") (names nil))
  "Resolve a set of names to IDs in the following categories: agents, alliances, characters, constellations, corporations factions, inventory_types, regions, stations, and systems. Only exact matches will be returned. All names searched for are cached for 12 hours

ESI Endpoint: POST /universe/ids/
Operation ID: post_universe_ids
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  :LANGUAGE (STRING) - Optional. Language to use in the response, takes precedence over Accept-Language [default: \"en\"] [values: \"en\", \"en-us\", \"de\", \"fr\", \"ja\", \"ru\", \"zh\", \"ko\", \"es\"]
  BODY (ARRAY) - Required. The names to resolve

Response Type: OBJECT
Response: List of id/name associations for a set of names divided by category. Any name passed in that did not have a match will be ommitted

Example:
  (post-universe-ids client)"
  (progn
   (progn
    (when *validate-parameters-p*
      (when language (check-type language string))
      (when language
        (unless (member language '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es") :test #'equal)
          (error 'esi-bad-request :message
                 (format nil "~A: ~S not in allowed values ~S" "language" language
                         '("en" "en-us" "de" "fr" "ja" "ru" "zh" "ko" "es"))))))
    (when *validate-parameters-p* (check-type names (or list vector))))
   (let ((request-path "/universe/ids/")
         (query-params
          (remove nil (list (when language (cons "language" (princ-to-string language)))))))
     (let ((response
            (http-request client request-path :method :post :query-params query-params :content
                          (when names (com.inuoe.jzon:stringify names)))))
       (values (esi-response-body response) response)))))


(defun post-universe-names (client &key (ids nil))
  "Resolve a set of IDs to names and categories. Supported ID's for resolving are: Characters, Corporations, Alliances, Stations, Solar Systems, Constellations, Regions, Types, Factions

ESI Endpoint: POST /universe/names/
Operation ID: post_universe_names
Category: universe
Authentication: Not required (public endpoint)

Parameters:
  BODY (ARRAY) - Required. The ids to resolve

Response Type: ARRAY of OBJECT
Response: List of id/name associations for a set of IDs. All IDs must resolve to a name, or nothing will be returned

Example:
  (post-universe-names client)"
  (progn
   (progn (when *validate-parameters-p* (check-type ids (or list vector))))
   (let ((request-path "/universe/names/"))
     (let ((response
            (http-request client request-path :method :post :content
                          (when ids (com.inuoe.jzon:stringify ids)))))
       (values (esi-response-body response) response)))))

